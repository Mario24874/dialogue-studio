[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# ── Headers de seguridad ────────────────────────────────────────────────────
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# ── Assets estáticos Next.js: inmutables (content-hash en nombre) ───────────
[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ── Webhook de Stripe: sin caché ─────────────────────────────────────────────
[[headers]]
  for = "/api/stripe/webhook"
  [headers.values]
    Cache-Control = "no-store"

# ── Path mapping para assets estáticos de Next.js ────────────────────────────
# Con publish = ".next", el CDN sirve .next/ como raíz.
# .next/static/ está en CDN como /static/ (no como /_next/static/).
# El HTML de Next.js siempre referencia /_next/static/, por eso este rewrite.
[[redirects]]
  from = "/_next/static/*"
  to = "/static/:splat"
  status = 200
  force = true

# ── Rutas de Clerk ────────────────────────────────────────────────────────────
[[redirects]]
  from = "/sign-in"
  to = "/sign-in"
  status = 200

[[redirects]]
  from = "/sign-up"
  to = "/sign-up"
  status = 200
